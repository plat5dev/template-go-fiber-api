name: go-fiber-api
description: Go + Fiber reference API (profiles, projects, tasks)

upstreams:
  api: "3000"

routes:
  - ./routes.identity.yml
  - ./routes.yml

substitute:
  - go.mod

next:
  - go mod tidy
  - go run .
